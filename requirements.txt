# Create the file with essential packages
@"
numpy==1.24.4
pandas==2.0.3
oandapyV20==0.7.2
python-dotenv==1.0.0
"@ | Out-File -FilePath requirements.txt -Encoding utf8

# Verify it was created
Get-Content requirements.txt